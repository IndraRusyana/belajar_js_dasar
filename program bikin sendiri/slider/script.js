document.body.addEventListener('click', function(e) {
	const getSlider = document.querySelector('.slider');
	for (var i = 0; i < getSlider.length; i++) {
		
		if ( e.target.className == 'left' &&   ) {
		getSlider.style.right = '258px';	
		}
		if else () {

		}
	}
	
});